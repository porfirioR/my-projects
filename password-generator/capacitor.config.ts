import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myproyects.passgen',
  appName: 'Password Generator',
  webDir: 'dist'
};

export default config;
